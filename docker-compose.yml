services:
  graph-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: graph-api-v2
    ports:
      - "${GRAPH_API_PORT:-18080}:8080"
    environment:
      GRAPH_API_DUCKDB_FILE: "${GRAPH_API_DUCKDB_FILE:-finbench_sf0_1.duckdb}"
      SPRING_DATASOURCE_URL: jdbc:duckdb:/data/graph_api/${GRAPH_API_DUCKDB_FILE:-finbench_sf0_1.duckdb}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: "2"
      SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE: "1"
      SPRING_DATASOURCE_ALLOW_UNSIGNED_EXTENSIONS: "true"
      SPRING_FLYWAY_ENABLED: "false"
      GRAPH_QUERY_BACKEND: "DUCKPGQ"
      GRAPH_DUCKPGQ_ENABLED: "true"
      GRAPH_DUCKPGQ_AUTO_LOAD: "true"
      GRAPH_DUCKPGQ_PREFER_LATEST: "false"
      GRAPH_CORS_ALLOWED_ORIGINS: "http://localhost:3000,http://localhost:5173,http://127.0.0.1:5173"
      JAVA_OPTS: "-XX:+UseContainerSupport -XX:MaxRAMPercentage=75.0"
      HOME: /data/graph_api
    entrypoint:
      - sh
      - -c
      - |
        DB_FILE="$${GRAPH_API_DUCKDB_FILE:-finbench_sf0_1.duckdb}"
        DB_PATH="/data/graph_api/$${DB_FILE}"
        if [ ! -f "$${DB_PATH}" ]; then
          echo "FinBench DuckDB file not found: $${DB_PATH}" >&2
          echo "Prepare it with scripts/finbench-data.sh, scripts/finbench-data.ps1, or scripts/finbench-docker.ps1 before starting Docker." >&2
          exit 1
        fi
        exec java $${JAVA_OPTS} -jar /app/app.jar
    volumes:
      - ./data:/data/graph_api
    restart: unless-stopped

  neo4j:
    image: neo4j:5-community
    container_name: graph-api-v2-neo4j
    profiles: ["neo4j"]
    environment:
      NEO4J_AUTH: neo4j/graph-api-password
    ports:
      - "7474:7474"
      - "7687:7687"
    volumes:
      - graph_api_neo4j_data:/data
    restart: unless-stopped

  memgraph:
    image: memgraph/memgraph:latest
    container_name: graph-api-v2-memgraph
    profiles: ["bench-candidates"]
    ports:
      - "7688:7687"
    volumes:
      - graph_api_memgraph_data:/var/opt/memgraph
    restart: unless-stopped

  postgres-age:
    image: apache/age:latest
    container_name: graph-api-v2-postgres-age
    profiles: ["bench-candidates"]
    environment:
      POSTGRES_DB: graph_bench
      POSTGRES_USER: graph
      POSTGRES_PASSWORD: graph-api-password
    ports:
      - "5433:5432"
    volumes:
      - graph_api_postgres_age_data:/var/lib/postgresql
    restart: unless-stopped

  arangodb:
    image: arangodb:3.12
    container_name: graph-api-v2-arangodb
    profiles: ["bench-candidates"]
    environment:
      ARANGO_ROOT_PASSWORD: graph-api-password
    ports:
      - "8529:8529"
    volumes:
      - graph_api_arangodb_data:/var/lib/arangodb3
      - graph_api_arangodb_apps:/var/lib/arangodb3-apps
    restart: unless-stopped

  janusgraph:
    image: janusgraph/janusgraph:latest
    container_name: graph-api-v2-janusgraph
    profiles: ["bench-candidates"]
    environment:
      JANUS_STORAGE_TIMEOUT: ""
      JAVA_OPTIONS_FILE: /dev/null
      JAVA_OPTIONS: "-Xms256m -Xmx1g -XX:+UseG1GC -Xss256k -Dgremlin.io.kryoShimService=org.janusgraph.hadoop.serialize.JanusGraphKryoShimService"
      gremlinserver.evaluationTimeout: "120000"
    ports:
      - "8182:8182"
    volumes:
      - graph_api_janusgraph_data:/var/lib/janusgraph
    restart: unless-stopped

volumes:
  graph_api_data:
  graph_api_neo4j_data:
  graph_api_memgraph_data:
  graph_api_postgres_age_data:
  graph_api_arangodb_data:
  graph_api_arangodb_apps:
  graph_api_janusgraph_data:
